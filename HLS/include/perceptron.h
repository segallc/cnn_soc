#ifndef _PERCEPTRON_H_
#define _PERCEPTRON_H_

#include <cmath>
#include "sizes.h"
#include "types.h"

int perceptron(dType img_in[MAX_SIZE], dType kernels[MAX_KERNEL_SIZE], dType img_out[MAX_SIZE]);

#endif